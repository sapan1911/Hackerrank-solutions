import math
import os
import random
import re
import sys

# Complete the angryProfessor function below.
def angryProfessor(k, a):
    
    for i in range(n):
       count=0
       for j in range(len(a)):
           if a[j]<=0:
               count=count+1
       if count>=k:
            result="NO"
       else:
            result ="YES" 
    return result 
    
